%h3 Job Developer Home
=@job_developer.full_name

%h3 Your Job Seekers
- if @job_developer.agency_relations.in_role_of(:JD).empty?
  %i There are no job seekers assigned to you yet.
- else


%h3 Job Seekers Without a Job Developer
%br
  .col-sm-9
    %table.table.table-hover
      %thead
        %tr
          %td
            %strong Name
          %td
            %strong Status
          %td
            %strong Last Login
      %tbody
        - eligible_job_seekers_for_role(@job_developer, :JD).each do |js|
          %tr
            %td
              = js.full_name
            %td
              = js.employment_status
            %td
              = js.last_sign_in_at
